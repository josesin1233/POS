{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "echo 'Testing DB connection...' && python -c \"import os; print('DATABASE_URL:', os.getenv('DATABASE_URL', 'NOT_SET'))\" && python -c \"import os; print('POSTGRES_PASSWORD:', os.getenv('POSTGRES_PASSWORD', 'NOT_SET'))\" && python manage.py migrate --noinput && echo 'Starting server...' && gunicorn dulceria_pos.wsgi --log-file -",
    "numReplicas": 1,
    "sleepApplication": false,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
