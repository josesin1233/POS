{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "echo '🚀 Starting Railway deployment...' && python manage.py migrate --noinput && echo '✅ Migrations completed' && python init_railway.py && echo '✅ Initial data loaded' && gunicorn dulceria_pos.wsgi --log-file -",
    "numReplicas": 1,
    "sleepApplication": false,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
